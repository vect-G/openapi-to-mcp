# Pinned Issue Draft: Drop Your OpenAPI Here

Title:

`Drop your OpenAPI/Swagger here — I’ll make openapi-to-mcp support it` 

Body:

If `openapi-to-mcp` fails on your real-world spec, drop it in this thread.

What to include:

- Spec URL (public) or minimal private snippet
- Command you ran
- Error output
- Expected behavior

Fast path:

- Open a new issue with template: **OpenAPI Compatibility Request**
- Link it in this thread

Current focus:

- complex `$ref`
- Swagger 2 edge cases
- auth + request body edge cases

I will prioritize reproducible reports and add regression tests.
